# AgentVault Roadmap

## 0. Mission

AgentVault provides structured, bounded, verifiable agent-to-agent coordination
with privacy-preserving outputs.

AgentVault is the "everyday lane" sibling to hardened VCAV-H. It prioritises:

- Mechanically enforceable bounded outputs
- Verifiable execution via receipts
- Low operational complexity
- Extensibility without protocol rewrites

---

# I. Design Principles (Non-Negotiable)

These principles guide all roadmap decisions.

## 1. Bounded Disclosure is Mechanical, Not Normative

Privacy is not enforced by prompt instructions.

It is enforced by:

- Structured output schemas
- Deterministic validation
- Explicit entropy constraints
- Policy rules (where applicable)

Prompt programs are guidance only.

## 2. All Enforcement-Relevant Artefacts Are First-Class

If it affects what can come out of the vault, it must be:

- Explicit
- Versioned
- Content-addressed (hashable)
- Bindable into receipts

This includes:

- Contracts
- Output schemas
- Prompt programs
- Model profiles
- Policy bundles (future)
- Guardian configuration (future)

## 3. Contracts Reference Artefacts, Not Behaviour

No hidden logic such as:

```
if contract_type == "COMPATIBILITY" then do X
```

Instead:

- Contract selects schema
- Contract selects prompt program
- Contract selects model profile
- Contract selects policy bundle (future)

Behaviour derives from referenced artefacts.

## 4. Enforcement is Deterministic and Explicitly Classified

Any rule that affects output validity must:

- Be reproducible offline
- Be reflected in receipts (if release-gating)
- Not depend on model interpretation

Enforcement rules must be explicitly classified as either:

- **GATE** (release-blocking)
- **ADVISORY** (non-blocking, diagnostic only)

The classification must be visible in code or configuration (e.g. in PolicyBundle
metadata) to prevent silent drift over time.

## 5. Receipts Bind the Consent Boundary

Receipts must bind enough information to answer:

- What contract was executed?
- What schema was enforced?
- What model profile was used?
- What prompt program was used?
- What policy bundle was enforced? (future)
- What runtime built this?

Receipts should never imply guarantees stronger than what the system actually
enforces.

## 6. Novel Contracts Are Allowed. Novel Powers Are Not.

Users may define new contracts and schemas.

They may not introduce new classes of enforcement without upgrading the guardian
or relay.

This preserves extensibility without uncontrolled surface expansion.

---

# II. Roadmap Phases

---

# Phase 1 — Immediate Hardening

Scope: Close red-team leak class and remove major footguns.

## 1. All-Enum COMPATIBILITY Schema (v2) — DONE

*Completed: PR #20, merged 2026-02-26.*

- Removed free-text `overlap_summary`
- Introduced orthogonal enum dimensions (`thesis_fit`, `size_fit`, `stage_fit`,
  `confidence`, `primary_reasons`, `blocking_reasons`, `next_step`)
- Added bounded reason arrays (`uniqueItems: true`, `maxItems: 3` / `2`)
- Set `additionalProperties: false`
- Bumped schema version and contract hash
- Entropy budget: 32 bits (advisory); actual: ~22 bits

## 2. Relay-Side Deterministic Digit/Currency Guard — DONE

*Completed: relay-side GATE rule using Unicode categories Nd (numeric) and Sc
(currency symbols). Scoped to `vcav_e_compatibility_signal_v2` via hardcoded
schema ID. Constant-shape error response (`OUTPUT_POLICY_VIOLATION`). No
offending content in logs. Schema-first invariant regression-proofed by unit test.*

Scoped initially to COMPATIBILITY v2:

- After schema validation, reject if any string value contains:
  - Unicode numeric characters (category Nd — covers ASCII, fullwidth, Arabic-Indic)
  - Unicode currency symbols (category Sc — covers £, $, €, ¥, ₹, etc.)
- Abort with constant-shape error

This is a **GATE** rule.

Purpose: Defense-in-depth backstop / schema regression detector. The primary
privacy control is the all-enum schema with `additionalProperties: false`.
This guard fires only if the schema is misconfigured, weakened, or a provider
structured-output bug bypasses enum constraints.

## 3. Model Profile Immutability — DONE

*Completed: 2026-02-26.*

Add a lockfile (`model_profiles.lock`) that pins each profile ID to its
expected content hash. Relay refuses to start if any profile's computed hash
does not match the lockfile entry.

This is simpler than hash-named files (Option A considered, rejected — adds
indirection for no enforcement gain over a lockfile, and breaks human
readability of profile directories). The lockfile is regenerated by an
explicit command (`cargo run --example gen_lockfile -- <dir>`), never silently.

- `validate_model_profile_lockfile(dir)` — startup validation, hard fail on mismatch
- `generate_model_profile_lockfile(dir)` — lockfile generation (explicit only)
- `examples/gen_lockfile.rs` — CLI wrapper
- `prompt_programs/model_profiles.lock` — committed, pins `api-claude-sonnet-v1`

## 4. Honest Runtime Hashing — DONE

*Completed: `build.rs` embeds git commit SHA at build time. `runtime_hash` is
`SHA256(GIT_SHA)`. `model_weights_hash` and `inference_config_hash` use honest
sentinel values reflecting the API-mediated relay (no local weights or inference).
Health endpoint returns `git_sha` field. Receipts no longer claim unverifiable
runtime provenance.*

Replace `SHA256("version-string")` with:

- Git commit SHA embedded at build time, or
- Docker image digest

Receipts must not claim unverifiable runtime provenance.

## 5. Phase 1 Exit Criteria

Phase 1 is complete when:

**Mechanical (must pass in CI):**
- Relay-side digit/currency guard has deterministic unit tests — **done** (7 unit tests, PR #22)
- Schema validation rejects `additionalProperties` — **done** (v2 schema has `additionalProperties: false`)
- Model profile startup hash check passes — **done** (lockfile validation, PR #24)

**CI infrastructure:** Rust CI now authenticates to private `vault-family-core`
dependency via `VAULT_CORE_TOKEN` secret (PR #22). All jobs green.

**Evidential (documented, not CI-gated):**
- Red-team suite passes across providers (20/20 — done, see
  `docs/red-team-evaluation-notes.md`)
- Accumulation tests operate purely on enum output (done)
- No numeric or currency tokens appear in SAFE contract output — **done**
  (relay-side enforcement, PR #22)

**Phase 1 status: COMPLETE.** All mechanical and evidential exit criteria met.

---

# Phase 2 — Artefact Discipline (Short-Term)

Scope: Align implementation with design principles without major architecture
shift.

## 6. Output Schema as Standalone Artefact

Currently:
- Schema embedded inside contract
- Referenced by `output_schema_id` (human-readable string)

Target:
- Standalone schema artefact
- Referenced by `output_schema_hash`
- Schema registry (simple JSON or directory-based)

Receipt includes `output_schema_hash`.

**Current state:** Not started. Schema is embedded in contract and bound
indirectly via contract hash. No separate content-addressing.

## 7. RelayEnforcementPolicy Phase A — DONE

*Completed: PR #26, merged 2026-02-26.*

Introduced `RelayEnforcementPolicy` as a first-class, content-addressed
artefact with lockfile validation and receipt binding. This is the relay's
local enforcement config — distinct from the vcav orchestrator's governance
`PolicyBundle` (which governs lanes, provenance, asymmetry, TTL, entropy budget).

**Phase A deliverables:**
- `RelayEnforcementPolicy` type with enum-based rules (`RuleType`,
  `RuleScopeKind`, `EnforcementClass` — no stringly-typed variants)
- RFC 8785 JCS content addressing (via `receipt_core::canonicalize_serializable`)
- Lockfile validation (fail-closed by default; dev override requires both
  `VCAV_ENV=dev` and `VCAV_ENFORCEMENT_LOCKFILE_SKIP=1`)
- Reverse lockfile check (disk → lockfile, not just lockfile → disk)
- Capability derivation from rules (not declared in JSON)
- Receipt binding: `guardian_policy_hash` = real enforcement policy content hash
- Example policy: `compatibility_safe_v1.json` with Nd/Sc unicode guards
- 28 unit tests covering serde, hashing, lockfile, capabilities, path traversal

**Phase A is "declared, not enforced":** the receipt proves which enforcement
policy was loaded at startup, but the hardcoded guard still runs independently.

**Remaining phases:**
- **Phase B:** Wire existing hardcoded guard to read from policy config
- **Phase C:** Add schema-content-addressing-dependent rules (requires item 6)

**Dependency:** Phase C requires item 6 (schema-as-standalone-artefact).

## 8. Capability Declaration in Policy

Policy bundle declares required capabilities:

- `schema_validation`
- `entropy_tracking`
- `reject_patterns`
- `constant_shape`

Relay must assert it supports declared capabilities.

This prevents silent semantic drift.

## 9. Schema Versioning & Migration Policy

Explicitly define:

- Schema hashes are immutable
- Contracts are bound to specific schema hashes
- No in-place migration of existing contracts
- Parties must negotiate schema version out-of-band

Backwards compatibility is achieved by coexistence, not mutation.

---

# Phase 2b — OpenClaw Integration (Parallel Track)

Scope: First real peer-to-peer vault session between independent agents.

Per `docs/plans/open_claw_agent_vault_mcp_port_brief_v_1.md`.

This track runs in parallel with Phase 2 artefact discipline. It depends on
Phase 1 being complete (v2 schema hardened) but does not depend on Phase 2.

## 10. OpenClaw Skill + mcporter Integration — DONE

*Completed: PR #25, merged 2026-02-26.*

- Created OpenClaw Skill (`skills/openclaw/agentvault/SKILL.md`)
- VPS deployment runbook (`docs/guides/openclaw-vps-runbook.md`)
- Skill covers: identity discovery, INITIATE/RESPOND flow, resume loop,
  completion, failure, session state file, protocol rules, display rules
- "Outputs are signals" framing and non-leak UX rules
- No native plugin port (mcporter indirection only)

## 11. Async Invites & Inbox — DONE

*Completed: PR #36, merged 2026-02-27.*

Relay-mediated async inbox so Alice can deposit an invite while Bob is offline,
and Bob discovers it later by polling the relay. Eliminates the "synchrony
coincidence" that previously required both agents to be online simultaneously.

**Deliverables:**
- `inbox_types.rs` — Versioned wire types (`version: "1"`), state machine
  (PENDING → ACCEPTED/DECLINED/CANCELED/EXPIRED, all terminal), caller-dependent
  token redaction
- `inbox.rs` — `InboxStore` with 7-day TTL, two-phase expiry (expire first,
  GC after 24h grace), SSE broadcast per recipient
- `inbox_handlers.rs` — 7 Axum handlers: POST /invites, GET /inbox,
  GET /invites/:id, POST accept/decline/cancel, GET /inbox/events (SSE)
- `agent_registry.rs` — Static agent auth (JSON file, fail-closed, constant-time
  token validation)
- `packages/agentvault-client/src/inbox.ts` — HTTP client functions
- `packages/agentvault-mcp-server/src/relay-inbox-transport.ts` —
  `RelayInboxTransport` implementing `AfalTransport`
- `relaySignal.ts` FSM integration — INITIATE/phaseInvite, RESPOND/phaseDiscover,
  phaseJoin with relay inbox path
- `tests/live/drive-inbox.sh` — Live test scenario with offline delay proof
- 141 unit tests, 34 integration tests, 27 TS tests

**Architecture decisions:**
- Types prototyped in relay crate, versioned, extract to VFC when stable (see item 11c)
- Accept-to-session bridge: idempotent, reuses `SessionStore::create`
- Constant-shape error responses (401 for InviteNotFound + Unauthorized)
- SSE is lossy (bounded broadcast buffer), GET is authoritative

## 11a. First Live Async Invite Session (Local End-to-End)

- Run `drive-inbox.sh` against a real provider (not mock)
- Natural-language prompt → full vault session → verifiable receipts
- Async invite flow — Alice deposits invite, Bob discovers on next poll
- No manual coordination, no prompt-based protocol hints
- Capture: receipts, session pointers, logs

Success criteria:
- Receipts verifiable
- No out-of-band coordination
- No synchrony coincidence — invite survives offline period
- Session fully completed via tool-mediated flow

VPS deployment is a separate operational concern — not needed for protocol validation.

## 11b. Inbox Hardening (Post-Live Validation)

Follow-up improvements identified during PR review. Not blocking for first live
session but should be addressed before production use.

- `relayFetch` timeout wrapping — throw descriptive error instead of raw `AbortError`
- Runtime validation of `res.json()` casts in inbox client (library boundary)
- `accept_invite` mutex contention — holds inbox lock during `SessionStore::create`.
  Fine for single-agent traffic; needs lock splitting for concurrent accept load.
- Persistent inbox storage (SQLite) — currently in-memory, lost on relay restart.

## 11c. Extract Inbox Protocol Types to VFC

**Depends on:** Item 11a (first live session validates wire format).

The inbox wire types are currently prototyped in the relay crate (`inbox_types.rs`)
with explicit `version: "1"` for future extraction. Once the wire format is proven
stable through live sessions:

1. Extract protocol-level types to `vault-family-core`:
   - `InviteStatus`, `InboxEventType`, `DeclineReasonCode` (enums)
   - `InviteSummary`, `InviteDetailResponse`, `InboxResponse` (wire format)
   - Request/response types (`CreateInviteRequest`, `AcceptInviteResponse`, etc.)
2. Keep relay-internal types in relay crate:
   - `Invite` (server-side storage with `pub(crate)` state machine fields)
   - `InboxStore`, `InboxStoreInner`
3. Align TypeScript client types with VFC definitions (codegen or shared schema)
4. Resolve `Contract` dependency direction — `CreateInviteRequest` references
   relay-internal `Contract` type, needs restructuring for VFC

**Not before live validation.** Premature extraction adds cross-repo coordination
cost for types that may still change.

## 12. Native OpenClaw Plugin (Optional, Phase 4+)

A TypeScript plugin could:
- Provide native tools instead of mcporter indirection
- Expose configuration via `openclaw.plugin.json`
- Add UI hints

This increases integration tightness but expands trusted surface.

Not required for first peer-to-peer test. Only pursue if mcporter path proves
limiting.

---

# Phase 3 — Registry & Versioning Infrastructure

Scope: Prepare for ecosystem-scale usage.

## 13. Formal Registries

Introduce registries for:

- Schemas
- Policy bundles
- Model profiles
- Prompt programs

Properties:
- Append-only
- Content-addressed
- ID separate from hash
- Deprecation without deletion

Trust model for registry discovery and key distribution is explicitly out of
scope for AgentVault (see Out of Scope section).

## 14. Receipt Completeness Improvements

Add explicit binding of:

- `output_schema_hash`
- `policy_bundle_hash`
- `runtime_build_hash` (real)
- `model_profile_hash` (already present)
- `prompt_program_hash` (already present)

Receipts must reflect actual enforced artefacts.

**Current state:** Receipts bind 24+ fields including contract hash, prompt
template hash, model profile hash, model identity, entropy, and budget usage.
Missing: `output_schema_hash`, `policy_bundle_hash`, real `runtime_build_hash`.

## 15. Extended Accumulation Experiment (N=20-100)

Red team testing at N=3 showed no accumulation (see decision log, 2026-02-26).
This is a negative result at low N, not a proof of resistance.

Run extended accumulation experiments:
- N=20, N=50, N=100 sessions per (scenario, provider) pair
- Scenarios 06 (naive), 07 (strategic), 08 (expert)
- Measure: signal entropy per session, cross-session narrowing, reconstruction
  accuracy vs random baseline
- Feed results back into entropy budget governance (item 7)

If narrowing is detected at higher N, the response is to tighten the entropy
budget or reduce enum cardinality — not to change the protocol.

**Current state:** Not started. `accumulate.sh` supports multi-session
analysis but has only been run at N=3. Infrastructure exists; experiment time
is the bottleneck.

## 16. Persistent Receipt Storage (Optional)

Currently relay is stateless.

Optional feature:
- Append-only receipt log
- Useful for audit deployments

Not required for core AgentVault UX.

---

# Phase 4 — Extensibility & Custom Contracts

Scope: Enable safe bespoke contracts.

## 17. Custom Contract Builder (Advanced UX)

Allow users to:

- Define new output schemas
- Define policy bundles
- Generate content hashes
- Publish to local registry namespace

All within existing capability boundaries.

## 18. SAFE vs RICH Contract Variants

For COMPATIBILITY and similar:

**SAFE:**
- Enum-only
- No free text
- Deterministic policy gate
- Strict entropy bounds

**RICH** (only allowed once PolicyBundle v1 exists):
- May include bounded free text
- Must define explicit entropy upper bound
- Must define `maxLength` and/or token constraints
- Must include deterministic reject patterns
- Must clearly declare reduced privacy guarantees

SAFE is default.

RICH contracts must be explicitly labeled and not silently substituted.

---

# III. Out of Scope (For AgentVault Lane)

These belong to VCAV-H or a future hardened lane:

- Hardware enclave attestation
- Threshold attestation
- Multi-operator consensus
- Cryptographic model execution proofs
- Canonical registry key distribution and trust bootstrap

AgentVault remains software-attested, relay-based.

---

# IV. Immediate Priorities

**Phase 1 — COMPLETE:**
1. ~~Add relay-side digit/currency guard (Phase 1, item 2)~~ — **done** (PR #22)
2. ~~Replace fake runtime hash with real build artefact (Phase 1, item 4)~~ — **done** (PR #23)
3. ~~Add model profile startup hash check (Phase 1, item 3)~~ — **done** (PR #24)

**Phase 2 progress:**
4. ~~OpenClaw skill + VPS runbook (Phase 2b, item 10)~~ — **done** (PR #25)
5. ~~RelayEnforcementPolicy Phase A (Phase 2, item 7)~~ — **done** (PR #26)

**Next priorities:**
6. ~~Async invites & inbox (Phase 2b, item 11)~~ — **done** (PR #36)
7. First live async invite session (Phase 2b, item 11a)
   — **Current state:** Skill, runbook, and async inbox all exist. Run `drive-inbox.sh`
   against a real provider to validate the wire format end-to-end.
8. Output schema as standalone artefact (Phase 2, item 6)
   — **Current state:** Schema embedded in contract, referenced by human-readable
   `output_schema_id`. No content-addressing.
9. RelayEnforcementPolicy Phase B — wire guard to policy config
   — **Current state:** Phase A complete. Hardcoded guard runs independently of policy.
10. Extract inbox protocol types to VFC (Phase 2b, item 11c)
   — **Blocked on:** item 11a (live session validates wire format).

Stop there before expanding scope.

---

# V. Decision Log

| Date | Finding | Resolution | Principle |
|------|---------|------------|-----------|
| 2026-02-25 | Free-text `overlap_summary` leaked Alice's investment range (`£40K-£90K`) via Anthropic Sonnet 4.5 (scenario 06, v1 schema) | Remove free-text channel; enforce bounded enum output (v2 schema) | Privacy must be mechanically enforceable |
| 2026-02-26 | v2 all-enum schema: 20/20 red team runs pass across 7 scenarios × 2 providers. Same model that leaked under v1 produces zero leaks under v2. | Schema-level constraint is the primary privacy control, not model alignment. Documented in `docs/red-team-evaluation-notes.md`. | Privacy is a protocol property, not a model property |
| 2026-02-26 | Anthropic shows signal drift under adaptive accumulation (scenarios 07, 08); OpenAI perfectly stable. Drift is bounded (~22 bits) and non-monotonic. | Drift affects utility consistency, not privacy. No accumulation observed at N=3. Longer experiments needed (N=20-100). | Bounded channels limit accumulation even when model behaviour varies |
| 2026-02-26 | Model profile immutability: Option A (hash-named files) vs Option B (lockfile). Option A adds indirection and breaks human readability; Option B gives equivalent enforcement with a simpler model. | Use lockfile approach (Option B). Relay refuses to start on hash mismatch. Lockfile regenerated by explicit CLI command only. | Simplest mechanism that achieves the enforcement goal |
| 2026-02-26 | Digit/currency guard is defense-in-depth, not primary privacy control. Fires only on schema regression or provider structured-output bug. | Documented threat model explicitly in code and decision log. Guard uses Unicode categories (Nd, Sc) not ASCII-only. Hardcoded schema ID for Phase 1; migrates to PolicyBundle in Phase 2. | Defense-in-depth must be explicitly classified to prevent over-trust |
| 2026-02-27 | Relay sessions required synchrony coincidence — both agents online simultaneously. Blocked real-world usage. | Added relay-mediated async inbox with durable invites (7-day TTL), fail-closed agent auth, constant-shape errors. Types prototyped in relay crate with `version: "1"` — extract to VFC only after live session validates wire format. | Simplest mechanism that achieves the enforcement goal |
